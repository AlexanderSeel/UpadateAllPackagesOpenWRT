name: Build and release OpenWrt packages

on:
  workflow_dispatch:

permissions:
  contents: write

jobs:
  migration-placeholder:
    runs-on: ubuntu-24.04
    steps:
      - run: echo "Package source migration in progress"
